import { NonterminalKind } from '@nomicfoundation/slang/cst';
import { getNodeMetadata } from '../slang-utils/metadata.js';
import { Identifier } from './Identifier.js';
export class IdentifierPath {
    constructor(ast) {
        this.kind = NonterminalKind.IdentifierPath;
        const metadata = getNodeMetadata(ast);
        this.items = ast.items.map((item) => new Identifier(item));
        this.separators = ast.separators.map((separator) => separator.unparse());
        this.comments = metadata.comments;
        this.loc = metadata.loc;
    }
    print(path, print) {
        return path
            .map(print, 'items')
            .map((item, index) => index === 0 ? item : [this.separators[index - 1], item]);
    }
}
//# sourceMappingURL=IdentifierPath.js.map